# Clone your repo (if not already)
git clone https://github.com/Flip1-k/tracker2.git
cd tracker2

# Create a new server.js file with the code above
# (copy and paste the code into server.js)

# Commit and push
git add server.js
git commit -m "Fix: Remove duplicate express import and ensure error-free code"
git push origin main
